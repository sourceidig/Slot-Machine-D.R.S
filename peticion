Hay un error en el cálculo del RTP en todo el sistema. La fórmula actual es incorrecta:
python# INCORRECTO (fórmula actual)
rtp = round(salida / entrada * 100, 1)

# CORRECTO (fórmula que debe quedar)
rtp = round((entrada - salida) / entrada * 100, 1) if entrada > 0 else 0
La fórmula correcta representa la ganancia del local como porcentaje de lo jugado. Positivo = local ganó, negativo = local perdió.
Busca y reemplaza esta fórmula en TODOS los archivos del proyecto donde aparezca, principalmente en control/views.py (líneas aprox. 203, 205, 207, 1648, 1658, 1769, 1790, 1800, 4433) y también en control/utils.py y cualquier template donde se calcule RTP con JavaScript o Jinja. No dejes ninguna instancia sin corregir.