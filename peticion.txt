En control/views.py, función cuadratura_diaria_export_excel,
buscar la línea donde se construye el nombre de la hoja (nombre_base),
que actualmente usa c.fecha.strftime('%d/%m/%Y').

Reemplazar ese strftime por '%d-%m-%Y' (guiones en vez de barras):

  nombre_base = f"{c.fecha.strftime('%d-%m-%Y')} {c.sucursal.nombre}"[:28]

Además, como medida extra de seguridad, agregar justo después una línea
que elimine cualquier carácter inválido para nombres de hoja de Excel
(/ \ ? * [ ] : son todos inválidos en openpyxl):

  import re
  nombre_base = re.sub(r'[/\\?*\[\]:]', '-', nombre_base)[:28]

No tocar nada más.