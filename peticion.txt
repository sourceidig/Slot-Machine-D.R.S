Lee templates/controles/detail.html y templates/control/control_generado.html.

En AMBOS archivos, buscar todos los lugares donde se muestra el RTP
(buscar por "rtp", "RTP", "text-success", "text-danger" relacionados a RTP)
y reemplazar la lógica de colores por esta regla uniforme:

  Verde    → rtp > 15 AND rtp < 26        (15.01 a 25.99)
  Amarillo → rtp >= -5.01 AND rtp <= 15
             OR rtp >= 26 AND rtp <= 45   (incluye el 0%)
  Rojo     → rtp <= -5 OR rtp >= 45

Usar {% if %} / {% elif %} encadenados para evitar ambigüedad con and/or:

  {% if valor_rtp > 15 and valor_rtp < 26 %}
    class="text-success"
  {% elif valor_rtp <= -5 or valor_rtp >= 45 %}
    class="text-danger"
  {% else %}
    class="text-warning"
  {% endif %}

Donde "valor_rtp" es la variable de RTP en cada template
(puede llamarse rtp, l.rtp, item.rtp, etc. — adaptar al nombre real).

Aplicar a TODAS las celdas/spans de RTP en ambos archivos.
No tocar nada más.