Modificar control/templates/controles/list.html para que las filas sean
clickeables y la tabla se vea más compacta.

CAMBIO 1 — Agregar estilos en el template:

  <style>
    .tabla-controles { font-size: 0.72rem !important; }
    .tabla-controles th {
      font-size: 0.65rem !important;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      padding: 5px 8px !important;
      white-space: nowrap;
    }
    .tabla-controles td {
      padding: 5px 8px !important;
      vertical-align: middle;
      font-size: 0.72rem !important;
    }
    .tabla-controles tbody tr {
      cursor: pointer;
      transition: background-color 0.15s ease;
    }
    .tabla-controles tbody tr:hover {
      background-color: #f0f9ff !important;
    }
  </style>

CAMBIO 2 — Agregar clase tabla-controles al <table>:

  <table class="table table-hover table-bordered align-middle mb-0 tabla-controles">

CAMBIO 3 — Hacer cada fila clickeable hacia el detalle.
Reemplazar el <tr> actual por:

  <tr onclick="window.location='{% url 'control:controles_detail' c.id %}'">

CAMBIO 4 — Eliminar la columna Acciones completa.
Quitar el <th class="text-center">Acciones</th> del thead.
Quitar el <td class="text-center"> con el botón Ver de cada fila.
Actualizar el colspan del empty state de 4 a 4 (ya está bien,
solo verificar que coincida con el número final de columnas).

CAMBIO 5 — Mejorar el badge del turno con colores más sutiles
(consistente con el listado de cajas):

  {% if c.turno.tipo_turno == 'Mañana' %}
    style="font-size:0.62rem; padding:2px 7px; border-radius:6px;
           background:#fef9c3; color:#854d0e;"
  {% elif c.turno.tipo_turno == 'Tarde' %}
    style="font-size:0.62rem; padding:2px 7px; border-radius:6px;
           background:#fed7aa; color:#9a3412;"
  {% else %}
    style="font-size:0.62rem; padding:2px 7px; border-radius:6px;
           background:#1e293b; color:#f1f5f9;"
  {% endif %}

No tocar views.py ni ningún otro archivo.