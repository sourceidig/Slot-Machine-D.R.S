Lee templates/cuadratura_diaria/create.html.

El nuevo layout de divs perdió la apariencia visual de tabla. 
Agregar estilos para que se vea estructurado en desktop y limpio en móvil.

Envolver toda la sección CAJA DIARIA en un card con bordes:

  <div style="border: 1px solid #e2e8f0; border-radius: 10px; 
              overflow: hidden; background: white;">
    ... todas las caja-row ...
  </div>

Actualizar el CSS de .caja-row y .caja-label:

  .caja-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 10px 16px;
    border-bottom: 1px solid #f1f5f9;
    background: white;
  }
  .caja-row:last-child {
    border-bottom: none;
  }
  .caja-row:nth-child(even) {
    background: #fafafa;
  }
  .caja-label {
    font-weight: 700;
    font-size: 0.78rem;
    color: #475569;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    min-width: 110px;
    padding-top: 8px;
    flex-shrink: 0;
  }
  .caja-inputs {
    flex: 1;
    min-width: 0;
  }

  /* SUBTOTAL y TOTAL con fondo diferenciado */
  .caja-row.subtotal {
    background: #f8fafc;
  }
  .caja-row.total-row {
    background: #e0f2fe;
    font-weight: bold;
  }

  @media (max-width: 480px) {
    .caja-row {
      flex-direction: column;
      gap: 6px;
      padding: 10px 12px;
    }
    .caja-label {
      padding-top: 0;
      min-width: unset;
    }
  }

Agregar class="subtotal" a la fila de SUBTOTAL y class="total-row" 
a la fila de TOTAL. No cambiar nada más.