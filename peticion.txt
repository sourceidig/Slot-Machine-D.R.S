Lee templates/cuadratura_diaria/create.html y control/urls.py.

BUG: El botón "Volver" y "Cancelar" en el formulario de edición de caja 
redirigen a la lista, pero deberían ir al detalle de la caja que se estaba 
editando.

FIX en create.html:
Buscar los botones/links de "Volver" y "Cancelar". Actualmente apuntan a 
algo como {% url 'control:cuadratura_diaria_list' %}.

En modo edición (cuando cuadratura.pk existe), cambiar el href a la vista 
de detalle de esa caja:

  {% if cuadratura.pk %}
    href="{% url 'control:cuadratura_diaria_detail' cuadratura.pk %}"
  {% else %}
    href="{% url 'control:cuadratura_diaria_list' %}"
  {% endif %}

Aplicar esto a AMBOS botones (Volver al inicio y Cancelar).

Verificar en urls.py cómo se llama la url del detalle 
(puede ser cuadratura_diaria_detail, cuadratura_detail, etc.) 
y adaptar el nombre exacto.

No tocar ninguna otra lógica.