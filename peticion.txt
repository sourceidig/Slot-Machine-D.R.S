Lee el archivo templates/cuadratura_diaria/create.html.

En la función `recalcUI()` hay un bug en el cálculo del descuadre. 
Las variables `cajaAnteriorVal` y `numeralDiaVal` ya se leen correctamente 
pero no se usan en la fórmula de descuadre.

La fórmula actual (INCORRECTA) es:
  const total = subtotal - redbank - transfer + prestamos;
  const descuadre = desglose - total;

La fórmula correcta debe ser idéntica a _recalcular_totales() en views.py:
  const totalCalculado = cajaAnteriorVal + numeralDiaVal + prestamos 
                         - sorteos - gastos - sueldos - regalos - jugados 
                         - redbank - transfer;
  const descuadre = desglose - totalCalculado;

IMPORTANTE: NO cambies la variable `total` ni el campo "TOTAL" que se muestra 
en el formulario (ese representa solo los movimientos del día y debe quedar igual).
Solo cambia la línea `const descuadre = desglose - total;` para que use 
`totalCalculado` con la fórmula correcta.

Después de hacer el cambio, verifica que la función recalcUI() quede así 
(solo las líneas relevantes):
1. Se mantiene: const total = subtotal - redbank - transfer + prestamos;
2. Se mantiene: setVal(totalEl, total);
3. Se agrega:   const totalCalculado = cajaAnteriorVal + numeralDiaVal + prestamos - sorteos - gastos - sueldos - regalos - jugados - redbank - transfer;
4. Se cambia:   const descuadre = desglose - totalCalculado;   // antes era: desglose - total

No toques nada más en el archivo.