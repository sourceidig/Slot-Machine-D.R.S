Lee CLAUDE.md. Solo modificar templates/cuadratura_diaria/create.html.

En el JS, dentro del bloque donde se procesa la respuesta AJAX exitosa 
(donde está setVal(cajaAnteriorEl, data.caja_anterior ?? 0)), 
envolver ESA línea específica con una condición para que solo actualice 
caja_anterior cuando NO estamos en modo edición (exclude_pk == 0):

Buscar:
        setVal(cajaAnteriorEl, data.caja_anterior ?? 0);

Reemplazar por:
        if (!excludePk || excludePk <= 0) {
          setVal(cajaAnteriorEl, data.caja_anterior ?? 0);
        }

La variable excludePk ya existe en el JS más arriba. No tocar nada más.
Commit, push y bash 

