En control/templates/cuadratura_diaria/create.html, buscar TODOS
los inputs readonly relacionados al turno en modo edición y cambiar
el value. Hay que buscar cualquier input que tenga en su value algo
como cuadratura.turno y reemplazarlo.

Buscar esta línea (o similar):
  value="{{ cuadratura.turno.sucursal.nombre }} — {{ cuadratura.turno.fecha|date:'d/m/Y' }} — {{ cuadratura.turno.tipo_turno }}"

Reemplazar por:
  value="{{ cuadratura.turno.tipo_turno|default:'Sin turno' }}"

Si no encuentra esa línea exacta, buscar cualquier input con
value que contenga "cuadratura.turno" y simplificarlo a solo
mostrar cuadratura.turno.tipo_turno.

Además, para el campo Fecha que también se ve cortado (muestra
"08/05/202" sin el año completo), buscar el input readonly de fecha
en modo edición y asegurarse que tenga min-width:115px:
  style="min-width:115px; background:#f1f5f9; font-weight:700;"

No tocar nada más.